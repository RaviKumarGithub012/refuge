/**
 * This is header component which include navigation and language toggle
 */

import React from "react";
import { withRouter } from 'react-router-dom';

const Header = () => {
  return <header>
   
  </header>;
};

export default withRouter(Header);
